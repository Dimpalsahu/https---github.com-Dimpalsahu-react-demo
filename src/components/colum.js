import React from 'react'

function Colum() {
    const item=[]
  return (
    <React.Fragment>
        {/* {


item.map( item =>(
    <React.Fragment  key ={item.id}>
        <h1>Title</h1>
        <p>{item.title}</p>
    </React.Fragment>
))
        } */}

    <td>NAme</td>
    <td>Dimpal</td></React.Fragment>


    // Inbetween div tag console give the warning show we can use react.fragment
 
  )
}

export default Colum